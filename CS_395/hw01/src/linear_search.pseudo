l = [a1, a2, ... an]
l2 = [] #empty 

for i in range(0, len(l)):
	#find min_val
	min_val = l[0]
	for j in range(0, len(l)):
		if l[j] < min_val:
			min_val = l[j]
	l.remove(min_val)
	l2.append(min_val)
